1. _可以用来指定将函数整体赋值给变量,例如:  var f  = fun _
2. _可以在匿名函数中表示函数的参数。
3. 在import操作中, _可以表示导入包下所有的类  ,例如: import java.util._
4. 在import操作中, _可以用来屏蔽包中的类 ,例如: import java.sql.{Date=>_ , _}
5. 在声明属性时， _可以用来做属性的默认初始化
6. 在模式匹配中,  _可以用来表示默认分支。 例如: case _
7. 在模式匹配中,  _可以用来表示集合中的一个元素.